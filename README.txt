All the information relative to the functions:

Structure of the Firestore Database:

users => Id's:
            - collections => Id's
            - products => Id's
            - followers => Id's
            - following => Id's
            - news => Id's




Abreviattions:
    promise => prom;
    remove => rm;
    User => us;
    List => lst;
    Product => prod;
    Owner => own;
    Follower => fllr;
    Following => fllg;
    Collection => coll;
    Snapshot => snap;
    Context => ctx;
    Update => upd;

